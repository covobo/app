nelmio_api_doc:
    documentation:
        info:
            title: Home task
            description: SunFinanceGroup home task
            version: 1.0.0
    areas:
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
            - ^/templates # Accepts routes under /templates
            - ^/verifications # Accepts routes under /verifications
